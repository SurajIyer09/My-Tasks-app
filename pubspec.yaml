name: todo_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # Added for icon generation
  flutter_launcher_icons: "^0.13.1"

flutter:
  uses-material-design: true

  # If you want to use the icon inside your app screens (not just as the launcher icon),
  # uncomment the lines below:
  # assets:
  #   - assets/icon/

# --- Icon Configuration ---
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21